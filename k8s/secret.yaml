# Template only - placeholder values. Real deployments must inject DATABASE_URL
# and JWT_SECRET via a secrets manager (AWS Secrets Manager, sealed-secrets,
# external-secrets, etc). Never commit real values to this file.
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgres://taskflow:CHANGEME@postgres:5432/taskflow?sslmode=disable"
  JWT_SECRET: "CHANGEME"
